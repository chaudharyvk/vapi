# Vapi Configuration
# Get your API key from: https://dashboard.vapi.ai/
NEXT_PUBLIC_VAPI_API_KEY=your_api_key_here

# Get your assistant ID from: https://dashboard.vapi.ai/assistants
# This should be a UUID like: 01234567-89ab-cdef-0123-456789abcdef
NEXT_PUBLIC_VAPI_ASSISTANT_ID=your_assistant_id_here

# Private server-side API key (recommended for Next.js API routes)
# Do NOT expose this in the browser. Use for server-side fetches only.
VAPI_SERVER_API_KEY=your_private_server_key_here

# Optional: Vapi Server URL (defaults to production)
# NEXT_PUBLIC_VAPI_BASE_URL=https://api.vapi.ai

# --- GCP for chunk uploads (server-side only) ---
# Prefer GOOGLE_CLOUD_* vars. GCP_* equivalents are still supported for backward compatibility.
# Required
GOOGLE_CLOUD_PROJECT_ID=your_gcp_project_id
GOOGLE_CLOUD_BUCKET_NAME=vapi_video_recording
# Optional public base URL (defaults to https://storage.googleapis.com)
GOOGLE_CLOUD_STORAGE_BASE_URL=https://storage.googleapis.com

# Provide one of the following credential options:
# 1) Raw JSON of the service account (paste as a single line; escape newlines in private_key as \n)
#    Tip: Keep quotes and braces intact.
GOOGLE_APPLICATION_CREDENTIALS_JSON={"type":"service_account","project_id":"...","private_key_id":"...","private_key":"-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n","client_email":"...","client_id":"...","auth_uri":"https://accounts.google.com/o/oauth2/auth","token_uri":"https://oauth2.googleapis.com/token","auth_provider_x509_cert_url":"https://www.googleapis.com/oauth2/v1/certs","client_x509_cert_url":"..."}

# 2) Base64-encoded JSON of the same content as above
GCP_SERVICE_ACCOUNT_KEY_BASE64=base64_of_service_account_json

# 3) Split credentials (client email + PEM private key in an env var)
#    IMPORTANT: Escape newlines in the PEM with \n
GOOGLE_CLOUD_CLIENT_EMAIL=service-account@your-project.iam.gserviceaccount.com
# PEM private key content (escaped newlines). Example:
# -----BEGIN PRIVATE KEY-----\nMIIEvAIBADANBgkqhkiG9w0BAQEFAASCBK...\n...\n-----END PRIVATE KEY-----\n
# Although named like the standard file path var, this app treats this as PEM contents when JSON is not supplied.
GOOGLE_APPLICATION_CREDENTIALS=-----BEGIN PRIVATE KEY-----\nYOUR_ESCAPED_PEM_CONTENT\n-----END PRIVATE KEY-----\n
# --- Backward compatible GCP_* vars (optional) ---
GCP_PROJECT_ID=your_gcp_project_id
GCP_BUCKET_NAME=vapi_video_recording
# Raw JSON (paste as one line or keep newlines escaped)
GCP_SERVICE_ACCOUNT_JSON={"type":"service_account","project_id":"...","private_key_id":"...","private_key":"-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n","client_email":"...","client_id":"..."}
